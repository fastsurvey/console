import {initialize} from './initialize';
import {newId} from './new-id';
import {remove} from './remove';

const localIdUtils = {
    remove,
    newId,
    initialize,
};

export default localIdUtils;
