import {formOptions} from './form-options';
import {messages} from './messages';
import {initialReduxState} from './initial-redux-state';

const constants = {
    formOptions,
    messages,
    initialReduxState,
};

export default constants;
