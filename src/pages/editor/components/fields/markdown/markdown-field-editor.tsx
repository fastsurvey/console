import React from 'react';
import {types} from '/src/types';

function MarkdownFieldEditor(props: {
    value: string;
    setValue(v: string): void;
    disabled: boolean;
}) {
    return <div>MarkdownFieldEditor</div>;
}

export default MarkdownFieldEditor;
